language: php
php:
  - '7.4'

install:
  - composer install --prefer-dist --no-suggest

script:
  - composer ci
  - composer phar-build
  - ./server &
  - composer server-test

cache:
  directories:
  - "$HOME/.composer/cache/files"
